const add = (a, b) => a + b
const subtract = (a, b) => a - b
const multiply = (a, b) => a * b
const divide = (a, b) => a / b

//put all variables or functions here that you want to be accessible globally
module.exports = {
    add, 
    subtract, 
    multiply, 
    divide 
}